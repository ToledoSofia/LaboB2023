#include <iostream>
#include <vector>
using namespace std;
void ordenarVectores(vector<int>v1, vector<int>v2){
    vector<int>ordenado;
    int i = 0 j = 0, cont1 = 0, cont0 = 0;
    while(i<v1.size() && i<v2.size()){
        while(j<v1.size() && j<v2.size()){
            if(){

            }

        }
    }

    
}
int main(){

}